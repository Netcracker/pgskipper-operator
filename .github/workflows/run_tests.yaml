name: Run Pgskipper Pipeline

permissions:
  contents: read
  packages: read
  actions: read

on:
  pull_request:
    types: [opened, synchronize, reopened]
    branches: [main]

jobs:
  # Wait-for-images:
  #   runs-on: ubuntu-latest
  #   strategy:
  #     fail-fast: false 
  #     matrix:
  #       workflow_name: 
  #         - build-backup-daemon.yaml
  #         - build-dbaas-adapter.yaml
  #         - build-monitoring-agent.yaml
  #         - build-operator.yaml
  #         - build-patroni.yaml
  #         - build-pgbackrest-sidecar.yaml
  #         - build-query-exporter.yaml
  #         - build-replication-controller.yaml
  #         - build-upgrade.yaml
  #   steps:
  #     - name: Wait for ${{ matrix.workflow_name }}
  #       uses: netcracker/qubership-workflow-hub/actions/wait-for-workflow@3e428f45132119ce49d90f3770a45d4fe1d1078d
  #       with:
  #         workflow: ${{ matrix.workflow_name }}
  #         token: ${{ secrets.GITHUB_TOKEN }}
  #         poll-interval: 20
  Pgskipper-Pipeline:
    # needs: Wait-for-images
    uses: Netcracker/qubership-test-pipelines/.github/workflows/pgskipper.yaml@86d0556d90790930af865a7c5b42f092259a6294
    with:
      repository_name: ${{ github.repository }}
      service_branch: '${{ github.head_ref || github.ref_name }}'
      versions_file: '.github/versions.yaml'
      pipeline_branch: '86d0556d90790930af865a7c5b42f092259a6294' #this value must match the value after '@' in 'uses'
    secrets:
      AWS_S3_ACCESS_KEY_ID: ${{secrets.AWS_S3_ACCESS_KEY_ID}}
      AWS_S3_ACCESS_KEY_SECRET: ${{secrets.AWS_S3_ACCESS_KEY_SECRET}}
