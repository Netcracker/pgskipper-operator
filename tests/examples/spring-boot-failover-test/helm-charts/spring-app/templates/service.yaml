apiVersion: v1
kind: Service
metadata:
  name: {{ include "postgresql-failover-test.fullname" . }}
  labels:
    {{- include "postgresql-failover-test.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "postgresql-failover-test.selectorLabels" . | nindent 4 }}
