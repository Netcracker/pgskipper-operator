apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "postgresql-failover-test.fullname" . }}-config
  labels:
    {{- include "postgresql-failover-test.labels" . | nindent 4 }}
data:
  database-url: "jdbc:postgresql://{{ .Values.database.hosts }}/{{ .Values.database.name }}?{{ .Values.database.jdbcParams }}"
