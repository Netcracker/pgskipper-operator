apiVersion: v1
kind: Secret
metadata:
  name: {{ include "postgresql-failover-test.fullname" . }}-secret
  labels:
    {{- include "postgresql-failover-test.labels" . | nindent 4 }}
type: Opaque
stringData:
  database-username: {{ .Values.database.username }}
  database-password: {{ .Values.database.password }}
